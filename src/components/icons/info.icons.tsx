import { iconWithClassName } from "@/lib/icons/iconWithClassName";
import {
	BoxIcon,
	Mail,
	Phone,
	MapPin,
	CreditCard,
	Calendar,
	TrendingUp,
} from "lucide-react-native";

iconWithClassName(BoxIcon);
iconWithClassName(Mail);
iconWithClassName(Phone);
iconWithClassName(MapPin);
iconWithClassName(CreditCard);
iconWithClassName(Calendar);
iconWithClassName(TrendingUp);

export { BoxIcon, Mail, Phone, MapPin, CreditCard, Calendar, TrendingUp };
