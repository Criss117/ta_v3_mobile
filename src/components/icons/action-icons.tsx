import { iconWithClassName } from "@/lib/icons/iconWithClassName";
import {
	Trash2Icon,
	PencilIcon,
	MoreVerticalIcon,
	X,
	ArrowLeft,
	Plus,
	LogOut,
	ChevronsUpDown,
} from "lucide-react-native";

iconWithClassName(Trash2Icon);
iconWithClassName(PencilIcon);
iconWithClassName(MoreVerticalIcon);
iconWithClassName(X);
iconWithClassName(ArrowLeft);
iconWithClassName(Plus);
iconWithClassName(LogOut);
iconWithClassName(ChevronsUpDown);

export {
	PencilIcon,
	Trash2Icon,
	MoreVerticalIcon,
	X,
	ArrowLeft,
	Plus,
	LogOut,
	ChevronsUpDown,
};
